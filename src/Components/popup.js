import React from 'react'

function popup(this.props.first) {
  return (
    <div className='popup'>popup
    <div className='pop-inner'>
        <button className='close-btn'></button>
        {props.children}</div></div>
  )
}

export default popup